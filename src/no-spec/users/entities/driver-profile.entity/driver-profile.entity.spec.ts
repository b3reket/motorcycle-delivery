import { DriverProfileEntity } from './driver-profile.entity';

describe('DriverProfileEntity', () => {
  it('should be defined', () => {
    expect(new DriverProfileEntity()).toBeDefined();
  });
});
